{% if is_granted('ROLE_ADMIN' or is_granted('ROLE_INSTRUCTOR')) %}
    {% block language_profile %}
        <ul class="list-unstyled topbar-menu float-end mb-0">
            <li class="dropdown notification-list topbar-dropdown d-none d-lg-block">
                <a class="nav-link dropdown-toggle arrow-none" data-bs-toggle="dropdown" id="topbar-languagedrop" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                    <img src="{{ asset('images/country/australia.png') }}" alt="user-image" class="me-1" height="12"> 
                    <span class="align-middle">English</span> 
                    <i class="bi bi-bell"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-end dropdown-menu-animated topbar-dropdown-menu" aria-labelledby="topbar-languagedrop" style="">
                    <!-- item-->
                    <a href="#" class="dropdown-item notify-item">
                        <img src="{{ asset('images/country/vietnam.png') }}" alt="user-image" class="me-1" height="12"> <span class="align-middle">Vietnam</span>
                    </a>

                    <!-- item-->
                    <a href="#" class="dropdown-item notify-item">
                        <img src="{{ asset('images/country/thailand.png') }}" alt="user-image" class="me-1" height="12"> <span class="align-middle">Thailand</span>
                    </a>
                </div>
            </li>

            <li class="dropdown notification-list">
                <a class="nav-link dropdown-toggle nav-user arrow-none me-0" data-bs-toggle="dropdown" id="topbar-userdrop" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                    <span class="account-user-avatar"> 
                        <img src="{{ asset('images/profile/alien.png') }}" alt="user-image" class="rounded-circle">
                    </span>
                    <span>
                        <span class="account-user-name">Dominic Keller</span>
                        <span class="account-position">
                            {% if is_granted('ROLE_ADMIN') %}
                                <strong> Admin </strong>
                            {% elseif is_granted('ROLE_INSTRUCTOR') %}
                                <strong> Instructor </strong>
                            {% else %}
                                <strong> Not verified </strong>
                            {% endif %}
                        </span>
                    </span>
                </a>
                <div class="dropdown-menu dropdown-menu-end dropdown-menu-animated topbar-dropdown-menu profile-dropdown" aria-labelledby="topbar-userdrop" style="">
                    <!-- item-->
                    <div class=" dropdown-header noti-title">
                        <h6 class="text-overflow m-0">Welcome !</h6>
                    </div>

                    <!-- item-->
                    <a href="#" class="dropdown-item notify-item">
                        <i class="mdi mdi-account-circle me-1"></i>
                        <span>My Account</span>
                    </a>

                    <!-- item-->
                    <a href="#" class="dropdown-item notify-item">
                        <i class="mdi mdi-account-edit me-1"></i>
                        <span>Settings</span>
                    </a>

                    <!-- item-->
                    <a href="#" class="dropdown-item notify-item">
                        <i class="mdi mdi-lifebuoy me-1"></i>
                        <span>Support</span>
                    </a>

                    <!-- item-->
                    <a href="#" class="dropdown-item notify-item">
                        <i class="mdi mdi-lock-outline me-1"></i>
                        <span>Lock Screen</span>
                    </a>

                    <!-- item-->
                    <a href="{{ path('app_logout') }}" class="dropdown-item notify-item">
                        <i class="mdi mdi-logout me-1"></i>
                        <span>Logout</span>
                    </a>

                </div>
            </li>
        </ul>
    {% endblock %}
{% endif %}


{% if not is_granted('ROLE_ADMIN') and not is_granted('ROLE_INSTRUCTOR')  %}
{% block login_register %}
    <ul class="list-unstyled topbar-menu float-end mb-0">
        <li class="nav-item notification-list d-none d-lg-block topbar-dropdown">
            <a style="color: #fff;" class="nav-link" href="{{ path('admin_login') }}">Admin Login</a>
        </li>
        <li class="nav-item notification-list d-none d-lg-block topbar-dropdown">
            <a style="color: #fff;" class="nav-link" href="{{ path('instructor_login') }}">Instructor Login</a>
        </li>
        <li class="nav-item notification-list d-none d-lg-block topbar-dropdown">
            <a style="color: #fff;" class="nav-link" href="{{ path('instructor_register') }}">Instructor Register </a>
        </li>
    </ul>
{% endblock %}
{% endif %}