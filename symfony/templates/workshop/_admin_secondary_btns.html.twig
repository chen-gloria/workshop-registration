<div class="dropdown card-widgets">
    <a href="#" class="dropdown-toggle arrow-none" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="dripicons-dots-3"></i>
    </a>
    <div class="dropdown-menu dropdown-menu-end">
        <form action="{{ path('admin_workshop_edit', { id: workshop.id }) }}" method="POST">
            <button class="dropdown-item"><i class="mdi mdi-pencil me-1"></i>Edit</button>
        </form>

        <button class="dropdown-item" data-bs-toggle="modal" data-bs-target="#warning-alert-modal"><i class="mdi mdi-delete me-1"></i>Delete</button>
        
        <button class="dropdown-item"><i class="mdi mdi-email-outline me-1"></i>Invite</button>
    </div>

    <div id="warning-alert-modal" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog modal modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-body p-4">
                    <div class="text-center">
                        <i class="dripicons-warning h1 text-danger"></i>
                        <h4 class="mt-3">You are going to delete this workshop, are you sure? </h4>
                        <form action="{{ path('admin_workshop_remove', { id: workshop.id }) }}" method="POST">
                            <button class="btn btn-danger my-2" data-bs-dismiss="modal" formmethod="post">Yes, delete it.</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>